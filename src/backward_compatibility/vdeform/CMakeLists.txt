PROJECT(vdeform)

ADD_EXECUTABLE(vdeform vdeform.C)
TARGET_LINK_LIBRARIES(vdeform lipsia ${VIA_LIBRARY})

SET_TARGET_PROPERTIES(vdeform PROPERTIES
    LINK_FLAGS -Wl)

INSTALL(TARGETS vdeform
        RUNTIME DESTINATION bin
        COMPONENT RuntimeLibraries)
