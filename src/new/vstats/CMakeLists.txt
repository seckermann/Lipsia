PROJECT(vstats)


ADD_EXECUTABLE(vstats vstats.c)
TARGET_LINK_LIBRARIES(vstats ${VIA_LIBRARY} ${BLAS_LIBRARY} ${LIPSIA_LIBRARY})

SET_TARGET_PROPERTIES(vstats PROPERTIES
    LINK_FLAGS -Wl)

INSTALL(TARGETS vstats
        RUNTIME DESTINATION ${LIPSIA_INSTALL_BIN_DIR}
        COMPONENT RuntimeLibraries)
