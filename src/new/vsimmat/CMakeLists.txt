PROJECT(vsimmat)


ADD_EXECUTABLE(vsimmat vsimmat.c)
TARGET_LINK_LIBRARIES(vsimmat ${VIA_LIBRARY} ${BLAS_LIBRARY} ${LIPSIA_LIBRARY})

SET_TARGET_PROPERTIES(vsimmat PROPERTIES
    LINK_FLAGS -Wl)

INSTALL(TARGETS vsimmat
        RUNTIME DESTINATION ${LIPSIA_INSTALL_BIN_DIR}
        COMPONENT RuntimeLibraries)
