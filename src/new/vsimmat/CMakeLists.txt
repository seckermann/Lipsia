PROJECT(vsimmat)


ADD_EXECUTABLE(vsimmat vsimmat.c)
TARGET_LINK_LIBRARIES(vsimmat ${VIAIO_LIBRARY} ${GSL_GSL_LIBRARY} ${GSL_CBLAS_LIBRARY} ${LIPSIA_LIBRARY})

SET_TARGET_PROPERTIES(vsimmat PROPERTIES
                      LINK_FLAGS -Wl)

INSTALL(TARGETS vsimmat
        RUNTIME DESTINATION bin
        COMPONENT RuntimeLibraries)
