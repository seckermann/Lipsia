PROJECT(vecm)


ADD_EXECUTABLE(vecm vecm.c)
TARGET_LINK_LIBRARIES(vecm ${VIA_LIBRARY} ${BLAS_LIBRARY} ${LIPSIA_LIBRARY})

SET_TARGET_PROPERTIES(vecm PROPERTIES
    LINK_FLAGS -Wl)

INSTALL(TARGETS vecm
        RUNTIME DESTINATION ${LIPSIA_INSTALL_BIN_DIR}
        COMPONENT RuntimeLibraries)
