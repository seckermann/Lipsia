PROJECT(vecm)


ADD_EXECUTABLE(vecm vecm.c)
TARGET_LINK_LIBRARIES(vecm ${VIAIO_LIBRARY} ${BLAS_LIBRARY} ${LIPSIA_LIBRARY})

SET_TARGET_PROPERTIES(vecm PROPERTIES
    LINK_FLAGS -Wl)

INSTALL(TARGETS vecm
        RUNTIME DESTINATION bin
        COMPONENT RuntimeLibraries)
