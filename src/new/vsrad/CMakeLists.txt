PROJECT(vsrad)

ADD_EXECUTABLE(vsrad vsrad.c SRAD.c)
TARGET_LINK_LIBRARIES(vsrad  ${VIA_LIBRARY} ${LIPSIA_LIBRARY})

SET_TARGET_PROPERTIES(vsrad PROPERTIES
    LINK_FLAGS -Wl)

INSTALL(TARGETS vsrad
        RUNTIME DESTINATION ${LIPSIA_INSTALL_BIN_DIR}
        COMPONENT RuntimeLibraries)
