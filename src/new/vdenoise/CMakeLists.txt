PROJECT(vdenoise)

ADD_EXECUTABLE(vdenoise vdenoise.c)
TARGET_LINK_LIBRARIES(vdenoise ${VIA_LIBRARY} ${LIPSIA_LIBRARY})

SET_TARGET_PROPERTIES(vdenoise PROPERTIES
    LINK_FLAGS -Wl)

INSTALL(TARGETS vdenoise
        RUNTIME DESTINATION ${LIPSIA_INSTALL_BIN_DIR}
        COMPONENT RuntimeLibraries)
