PROJECT(veta2)


ADD_EXECUTABLE(veta2 veta2.c)
TARGET_LINK_LIBRARIES(veta2 ${VIA_LIBRARY} ${BLAS_LIBRARY} ${LIPSIA_LIBRARY})

SET_TARGET_PROPERTIES(veta2 PROPERTIES
    LINK_FLAGS -Wl)

INSTALL(TARGETS veta2
        RUNTIME DESTINATION ${LIPSIA_INSTALL_BIN_DIR}
        COMPONENT RuntimeLibraries)
