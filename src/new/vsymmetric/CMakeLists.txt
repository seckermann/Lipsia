PROJECT(vsymmetric)


ADD_EXECUTABLE(vsymmetric vsymmetric.c)
TARGET_LINK_LIBRARIES(vsymmetric ${VIA_LIBRARY} ${BLAS_LIBRARY} ${LIPSIA_LIBRARY})

SET_TARGET_PROPERTIES(vsymmetric PROPERTIES
    LINK_FLAGS -Wl)

INSTALL(TARGETS vsymmetric
        RUNTIME DESTINATION ${LIPSIA_INSTALL_BIN_DIR}
        COMPONENT RuntimeLibraries)
