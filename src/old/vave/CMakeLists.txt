PROJECT(vave)

ADD_EXECUTABLE(vave vave.c)
TARGET_LINK_LIBRARIES(vave lipsia ${VIA_LIBRARY})

SET_TARGET_PROPERTIES(vave PROPERTIES
    LINK_FLAGS -Wl)

INSTALL(TARGETS vave
        RUNTIME DESTINATION ${LIPSIA_INSTALL_BIN_DIR}
        COMPONENT RuntimeLibraries)
